{
  "name": "design-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Worker xử lý design (download/hash/resize) trên server riêng — R2 lưu trữ, RabbitMQ nhận job. Xem documents/Plans/DesignStorage-R2-ProcessingWorker.md",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build-types": "tsc --noEmit -p tsconfig.json",
    "start": "node dist/index.js",
    "start:local": "tsc -p tsconfig.json && node dist/index.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "3.717.0",
    "amqplib": "^0.10.4",
    "dotenv": "^16.4.5",
    "mongoose": "^8.9.2",
    "nanoid": "^3.3.7",
    "shared": "workspace:*",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.3"
  }
}
