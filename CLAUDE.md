# OnosFactory — Coding Rules

> Đọc file này trước khi code bất kỳ thứ gì. Tuân thủ 100% các quy tắc bên dưới.

---

## Monorepo Structure

- **Package manager:** pnpm workspaces
- **Shared types:** Frontend và Backend cùng import từ `shared` package. Khi tạo mới DTO/enum/constant, đặt trong `packages/shared/`.

---

## Frontend Rules (apps/web)

> Chi tiết đầy đủ: [`apps/web/CLAUDE.md`](apps/web/CLAUDE.md)

---

## Backend Rules (apps/api)

> Chi tiết đầy đủ: [`apps/api/CLAUDE.md`](apps/api/CLAUDE.md)

---

## Shared Package Rules (packages/shared)

> Chi tiết đầy đủ: [`packages/shared/CLAUDE.md`](packages/shared/CLAUDE.md)

---

## General Rules

### TypeScript

- **Strict mode** — no `any` type trừ khi thật sự cần thiết.
- Dùng `import type { }` cho type-only imports.
- Interface naming: PascalCase, mô tả rõ ràng (e.g., `GetUsersResDto`, `FilterFormProps`).
- Enum pattern: `const` object with `as const` assertion.

### Git

- Commit message: English, concise, bắt đầu bằng verb (add, fix, update, remove, refactor).
- KHÔNG commit files chứa secrets (.env, credentials).

---

## Documentation Rules (documents/FunctionDescription)

> Mỗi tính năng / module có 1 file mô tả riêng trong `documents/FunctionDescription/`. **Doc phải đồng bộ với code.**

### Quy tắc cập nhật doc khi thay đổi code

- **Khi sửa bất kỳ file nào thuộc 1 tính năng** (FE page, BE module, shared DTO, helper liên quan) → **BẮT BUỘC** mở file doc tương ứng và cập nhật phần bị ảnh hưởng (schema, endpoint, UI component, flow, performance note...).
- **Khi thêm tính năng mới** → tạo 1 file `[FeatureName].md` mới trong `documents/FunctionDescription/` theo cùng cấu trúc các file hiện có (Overview → Luồng hoạt động → API/Schema → UI Components → Performance → Permissions), rồi **thêm 1 dòng vào bảng "Feature → Doc mapping" bên dưới**.
- **Khi xóa tính năng** → xóa file doc tương ứng và xóa dòng mapping.
- **Khi đổi tên file/route/endpoint** → grep `documents/FunctionDescription/*.md` và update các tham chiếu cũ.

### Feature → Doc mapping

> Bảng này là **bảng tra cứu chính**. Trước khi sửa code trong "Files / Folders liên quan", phải đọc file Doc tương ứng để hiểu context, và sau khi sửa phải update doc.

| Tính năng                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Doc                                                                                                              | Files / Folders liên quan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Dashboard** (6 tabs: Stats + Status + Factory + Lifecycle + Lỗi theo người + Designer)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | [`documents/FunctionDescription/Dashboard.md`](documents/FunctionDescription/Dashboard.md)                       | `apps/web/src/pages/home/` (`index.tsx` tabs, `OrderStatsTab.tsx`, `OrderStatusTab.tsx`, `OrderFactoryTab.tsx`, `PersonErrorTab.tsx`, `DesignerStatsTab.tsx`, `status/*.tsx`), `apps/api/src/modules/order/order.service.ts` → `getDashboard()` + `getStatusOverview()` + `getFactoryOverview()`, `apps/api/src/modules/designer/designer-stats.service.ts` (+ `getPersonErrorOverview()` + `getPersonErrorOrders()` + `getStageErrorDaily()`), `apps/api/src/modules/designer/designer-stats.controller.ts` (`GET /designer/person-error-overview` + `/person-error-orders` + `/fulfillment/stage-error-daily` + `/designer/product-time-overview` + `/designer/product-time-orders` — panel "Thời gian theo sản phẩm" mở từ nút Xem tất cả của widget Top Designer `TopDesigners.tsx`/`ProductTimePanel.tsx` + 2 cột Level/Gợi ý level sản phẩm 1..10 (`ProductConfig.level` sẵn có + `computeProductLevelSuggestions()` percentile 60 ngày, gán qua `PATCH /product-configs/:id` sẵn có); + `GET /designer/performance-scores` + `PATCH /designer/level/:userId` — bảng "Xếp hạng hiệu suất" `DesignerPerformanceCard.tsx` điểm 0-100 + hạng S-D `packages/shared/enums/designer-rank.ts` + `UserEntity.designerLevel`), `packages/shared/dtos/production-order.dto.ts`, `packages/shared/dtos/order-status.dto.ts`, `packages/shared/dtos/designer.dto.ts` (PersonError* + StageErrorDaily* DTOs). Tab "Lỗi theo người" 2 chiều — xem `documents/Plans/UniversalErrorReporting-PerPersonStageStats.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Vòng đời đơn** (Lifecycle dashboard — **MỌI tài khoản**, Fulfillment khóa xưởng; phễu rắn bò 8 chặng soát tool→thiết kế→6 stage fulfillment; lọc theo ngày vào sản xuất `inProductionAt`; snapshot + throughput theo kỳ. **Strip gọn** trên đầu Dashboard trên mọi tab: mặc định mini-phễu backlog, nhập `productionId` → hành trình 1 đơn)                                                                                                                                                                                                                                                                          | [`documents/FunctionDescription/OrderLifecycle.md`](documents/FunctionDescription/OrderLifecycle.md)             | `apps/web/src/pages/home/LifecycleTab.tsx` + `apps/web/src/pages/home/LifecycleStrip.tsx` (`index.tsx`: `<LifecycleStrip/>` trên đầu + `canSeeLifecycle=true`), `apps/web/src/services/order.ts` → `getLifecycleOverview` + `getLifecycleTrack` + `getCancelledOrders`, `apps/web/src/components/orders/CancelledOrdersDialog.tsx` (drill-down đơn hủy, mở từ LifecycleStrip chip "Hủy" + KPI "Đơn đã hủy" ở `OrderStatsTab.tsx`), `apps/api/src/modules/order/order.service.ts` → `getLifecycleOverview()` (+ `totals.cancelledInRange`) + `getLifecycleTrack()` + `getCancelledOrders()` + `toolCheckedAt` set tại updateField/bulkUpdateField/importRework + backfill `onModuleInit`, `apps/api/src/modules/order/order.entity.ts` (`toolCheckedAt`, `cancelledAt`, `cancelReason`), `apps/api/src/modules/order/order.controller.ts` → `GET /lifecycle-overview` + `GET /lifecycle-track/:code` + `GET /cancelled-list` (`@Auth` ORDER_VIEW_ROLES), `packages/shared/dtos/production-order.dto.ts` (`toolCheckedAt` + `LIFECYCLE_STAGE_KEYS` + Lifecycle Overview DTOs incl. `waitingToStart` + `cancelledInRange` + LifecycleTrack DTOs + `GetCancelledOrders`/`CancelledOrderRow` DTOs). **Loại đơn hủy (`cancelledAt`) khỏi mọi công đoạn + thống kê** — xem `documents/Plans/CancelledOrders-ExcludeFromStages.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Products** (Config + Xưởng + **Chi tiết sản phẩm/catalog + Biến thể** — Products.md §2.4/§2.5: `productCategoryId` (ref `ProductCategory` module riêng, §4)/`printMethod`/`printArea`/`mockupPrintUrl`/`sizeChartUrl`/`description`/`itemSpecifics`/đóng gói mặc định + `variations[]` (SKU unique toàn hệ thống, `cost`/`nonShipCost`/`retailPrice`)) | [`documents/FunctionDescription/Products.md`](documents/FunctionDescription/Products.md)                         | `apps/web/src/pages/products/`, `apps/api/src/modules/product-config/`, `apps/api/src/modules/factory/`, `apps/api/src/modules/machine-type/`, `apps/api/src/modules/product-category/` (§4), shared DTOs `product-config.dto.ts`, `factory.dto.ts`, `machine-type.dto.ts`, `product-category.dto.ts`, workshop_config category `print_method` (`WorkshopConfig.md`), `apps/web/src/components/settings/ProductFactoryKanban.tsx` (kanban kéo-thả gán xưởng theo sản phẩm ở `/adm/settings/product-factory` + cột "Chưa xác định xưởng" + nút Sync quét đơn 14 ngày `GET /product-configs/unmatched-order-types` → kéo vào xưởng TẠO config tối thiểu, `machineTypeId` đã nới optional + tooltip/preview mockup + nút "Lấy ảnh từ Onospod" `POST /product-configs/crawl-mockups` theo lô-cursor + nút Export Excel dạng file SKU — Products.md §2.6), `apps/web/src/pages/products/UploadConfigFileDialog.tsx` (nút "Tải config lên" — upload `.xlsx` dạng file SKU, so tên hiện chỉ sản phẩm CHƯA có config, chọn rồi thêm qua `POST /product-configs/import` — Products.md §2.1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Collections** (bộ sưu tập sản phẩm phẳng, 1 sản phẩm thuộc nhiều collection qua `ProductConfig.collectionIds` — chọn ở trang chi tiết sản phẩm, CRUD tab "Collection" trang Products; nền tảng cho catalog/đặt đơn theo sản phẩm) | [`documents/FunctionDescription/Collections.md`](documents/FunctionDescription/Collections.md) | `apps/api/src/modules/collection/`, `apps/web/src/pages/products/CollectionTab.tsx`, `apps/web/src/services/collection.ts`, `packages/shared/dtos/collection.dto.ts`, validate ref ở `apps/api/src/modules/product-config/product-config.service.ts` |
| **Promotion** (chương trình giảm giá theo tier khách hàng VIP 0..5 — quản lý/thống kê, giá tham khảo hiển thị ở Customer Portal Catalog, CHƯA tích hợp checkout; `scope='category'` tham chiếu `ProductCategory._id`, module riêng ở Products.md §4 — KHÔNG dùng workshop_config)                                                                                                                                                                                                                                                                                                                                                     | [`documents/FunctionDescription/Promotion.md`](documents/FunctionDescription/Promotion.md)                       | `apps/web/src/pages/promotions/` (`index.tsx` + `PromotionEditDialog.tsx`), `apps/web/src/services/promotion.ts` + `productCategory.ts`, `apps/api/src/modules/promotion/` (entity/repository/service/controller/module — `promotionMatches()`/`applyPromotionDiscount()`), `apps/api/src/modules/product-category/` (Products.md §4), `packages/shared/dtos/promotion.dto.ts` (`scopeCategoryId`) + `product-category.dto.ts`, `packages/shared/constants/permission-catalog.ts` (`page.promotions`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Orders** (List + Nhật ký bù lỗi + Workshop + Import + Cutting File Mapping + **Giữ đơn/Hold** + **Ưu tiên đơn/estimate**)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | [`documents/FunctionDescription/Orders.md`](documents/FunctionDescription/Orders.md)                             | `apps/web/src/pages/orders/` (`index.tsx`, `ListOrderTab.tsx`, `ErrorLogTab.tsx`, `OrderTableWorkshop.tsx`, `ImportOrderTab.tsx`, `ImportCuttingFilesTab.tsx`, `DesignerSummaryPanel.tsx`), `apps/web/src/components/orders/cells/` (incl. `AssigneeSelectCell`, `ProductionErrorSelectCell`, `ProductionErrorOtherDialog`, `ErrorSourceCell`, `PrioritySelectCell`), `apps/web/src/components/orders/BulkEditToolbar.tsx`, `apps/web/src/components/orders/AssignDesignerDialog.tsx`, `apps/web/src/components/orders/workshopTableConfig.tsx`, `apps/web/src/components/orders/OrderDetailDialog.tsx` (modal preview cuttingFile qua iframe Drive), \*\*Hold (Orders.md §9b):, `apps/web/src/components/orders/{HeldBadge,HoldOrderDialog,OrderRowActionsMenu}.tsx` + `apps/web/src/utils/orderActions.ts` (`isHeld`/`canUserHold`) + `services/order.ts` (`holdOrder`/`unholdOrder`/`bulkHold`); BE `order.service.ts` (`holdOrder`/`unholdOrder`/`bulkSetHold`/`assertNotHeld` + guard ở updateField/setProductionError + count `heldOrders`/`heldCount`) + guard ở `designer-task.service.ts`/`fulfillment-task.service.ts` transition + endpoints `POST /orders/:id/hold                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | unhold`+`PATCH /orders/bulk-hold`; `order.entity.ts` (`heldAt`/`holdReason`); `production-order.dto.ts`(Hold DTOs +`held`filter +`heldOrders`/`heldCount`); `order-log.dto.ts` (`hold`/`unhold`action)**, **Lấy ngược design/địa chỉ từ OnosPod cho đơn giữ (Orders.md §9c):`packages/shared/constants/hold-reason.ts` (`HOLD_REASON_WAITING_DESIGN`/`\_ADDRESS`, match CHÍNH XÁC với `holdReason`) + `apps/api/src/modules/order/onospod-order-lookup.service.ts` (`OnospodOrderLookupService.lookupByProductionId`— gọi`api.onospod.com`, khớp qua `line_items[].productions[].increment_id === productionId`, KHÔNG phải `product_id`) + `order.service.ts` (`recoverHeldOrders()`) + `order.entity.ts`/`production-order.dto.ts` (`shippingAddress`snapshot) +`api-config.service.ts` (`onospodApiConfig`, env `ONOSPOD_API_URL`/`\_BEARER_TOKEN`/`\_SUPER_TOKEN`) + endpoint public `GET /orders/recover-held-from-onospod/cron`**, **Ưu tiên (Orders.md §17): `packages/shared/enums/order-priority.ts` (`OrderPriority`) + `production-order.dto.ts` (`priority`field +`ORDER_PRIORITY_STAGE_ESTIMATE_HOURS`) + `apps/web/src/utils/priorityEstimate.ts` (`getStageDeadline`/`getActiveStageKey`) — tái dùng `updateField`/`bulkUpdateField` (field=`priority`), sort `priority: -1`ở`getOrders`/`getOrdersGroupedByType`/designer+fulfillment `getMyTasks`**, `apps/api/src/modules/order/`(trừ`getDashboard`; bao gồm `getErrorLog`+`productionFirstErrorAt`hook + backfill onModuleInit +`drive-file-name.service.ts`+`previewCuttingFiles`/`applyCuttingFiles`), `packages/shared/dtos/production-order.dto.ts`(trừ Dashboard zods; bao gồm`productionFirstErrorAt`+`GetErrorLogDto`/`Res`+`cuttingFileUrl/Name`+ Cutting File DTOs + util`parseProductionIdFromCuttingFilename`), `apps/api/src/utils/transform-drive-url.ts`(chia sẻ với Image module), **Design Review public API (Orders.md §18):`GET /orders/design-review/next`+`GET /orders/design-review/error-file-options`+`POST /orders/design-review/result`(cả 3`@Auth([], [], { public: true })`, không JWT) → `order.service.ts` (`getNextDesignReviewOrder()`claim+lease`designReviewClaimedAt`+`getDesignReviewErrorFileOptions()`đọc`workshop_config`category`error_file_type`+`setDesignReviewResult()`delegate`updateField()`tuần tự`toolResult`/`toolResultNote`/`errorFile`/`errorFileNote`) + `order/design-review-product-code.ts` (`mapProductTypeToCode()`) + `production-order.dto.ts` (`DesignReviewOrderZod`/`GetNextDesignReviewOrderResDto`+`DesignReviewErrorFileOptionZod`/`GetDesignReviewErrorFileOptionsResDto`+`SetDesignReviewResultZod`(`errorFile`/`errorFileNote`fields)/`SetDesignReviewResultResDto`) + `order.entity.ts` (`designReviewClaimedAt`)**, **"Không xác định xưởng" — đơn chưa map factoryId bị loại mặc định khỏi mọi view/API, chỉ xem qua menu riêng (Orders.md §19): `apps/web/src/pages/orders/unmapped/index.tsx`(route`/orders/unmapped`) + `apps/web/src/components/orders/AssignFactoryDialog.tsx`(extract từ`OrderFactoryTab.tsx`) — tái dùng `GET /orders?unmapped=true`+`PATCH /orders/bulk-assign`+ nút "Tự động gán xưởng" `POST /orders/remap-unmapped` (`remapUnmappedOrders()` re-map theo Product Config hiện tại, Orders.md §19.2); BE `order.service.ts` (`buildVisibilityFilter`default-exclude`factoryId`missing trừ`dto.unmapped===true`+ cùng pattern ở`getDashboard`/`getLifecycleOverview`/`getCancelledOrders`/`getErrorLog`/`getFactoryOverview`bỏ`unmappedCount`) + `fulfillment-task.service.ts` (`buildMyTaskBase`override-role) +`designer-stats.service.ts` (`getToolCheckOverview`/`getPersonErrorOverview`/`getStageErrorDaily`/`getAssignBacklog`/`getDailyOverview`/`getErrorStats`); permission `page.unmapped_factory`\*\*, **Xưởng ngoài luồng sản xuất — đơn xưởng US bị loại khỏi MỌI thống kê/danh sách/luồng (soát tool, auto-gán designer, entry fulfillment), CHỈ hiện ở tab Đơn hàng theo xưởng hoặc lọc tường minh factoryId (Orders.md §21): `apps/api/src/utils/excluded-factory.ts` (`EXCLUDED_PRODUCTION_FACTORY_SHORT_NAME='US'` + `productionFactoryClause`) + guard ở `order.service.ts` (buildVisibilityFilter/getDashboard/getLifecycleOverview/getCancelledOrders/getErrorLog/getNextDesignReviewOrder/autoAssignAfterImport/entry hooks + backfill) + `designer-stats.service.ts` (9 điểm) + `designer-task.service.ts` (`applyCompleteFulfillmentHook`) + `fulfillment-task.service.ts` (`buildMyTaskBase`)** |
| **Auth & Identity**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | [`documents/FunctionDescription/Auth.md`](documents/FunctionDescription/Auth.md)                                 | `apps/web/src/pages/login/`, `apps/web/src/pages/register/`, `apps/web/src/pages/account/`, `apps/web/src/pages/users/` (conditional factory dropdown khi role=Fulfillment), `apps/web/src/pages/roles/`, `apps/web/src/pages/custom-roles/`, `apps/web/src/pages/departments/`, `apps/web/src/store/authStore.ts`, **Ghi nhớ đăng nhập (Auth.md §2.3): `apps/web/src/store/sessionPersist.ts` (storage router local/session + session handoff sang tab mới + prefill email) + `customerAuthStore.ts` + `main.tsx` (handoff rồi `persist.rehydrate()` — 2 store bật `skipHydration`) + `pages/customer/login/index.tsx`; BE `auth.service.ts` `createAccessToken({rememberMe})` + env `JWT_REMEMBER_EXPIRATION_TIME`**, `apps/web/src/hooks/usePermission.ts`, `apps/web/src/components/roles/PermissionMatrix.tsx`, `apps/web/src/components/sidebar/Sidebar.tsx` (dynamic filter), `apps/api/src/modules/auth/`, `user/` (UserEntity field `factoryId`, `fulfillmentStage`, `telegramChatId`, `hireDate`, `designerLevel` — TUYỆT ĐỐI nhớ add field mới vào CẢ 2 $project `getUserById` + `getMe`, xem `Architecture/Common_Pitfalls.md §1`), `role/`, `custom-role/`, `permission/`, `departments/`, `packages/shared/constants/permission-catalog.ts`, `packages/shared/enums/role-type.ts` (`DesignerLeader`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Image Optimization & Caching**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | [`documents/FunctionDescription/ImageOptimization.md`](documents/FunctionDescription/ImageOptimization.md)       | `apps/api/src/utils/transform-drive-url.ts`, `apps/api/src/modules/order/order.service.ts` → `processDesigns()`, `refreshImageUrls()`, `apps/web/public/sw.js`, `apps/web/src/components/common/ImagePreviewDialog.tsx`, `apps/web/src/components/common/CopyButton.tsx`, `apps/api/src/modules/redis-cache/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Workshop Config** (danh mục xưởng)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | [`documents/FunctionDescription/WorkshopConfig.md`](documents/FunctionDescription/WorkshopConfig.md)             | `apps/web/src/pages/workshop-config/` (errorSource badge + form cho production_error), `apps/web/src/store/workshopConfigStore.ts`, `apps/web/src/services/workshopConfig.ts`, `apps/api/src/modules/workshop-config/` (auto-cleanup category=assignee + auto-backfill errorSource), `packages/shared/dtos/workshop-config.dto.ts`, `packages/shared/enums/workshop-config-category.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Order Log** (audit trail)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | [`documents/FunctionDescription/OrderLog.md`](documents/FunctionDescription/OrderLog.md)                         | `apps/api/src/modules/order-log/`, `apps/api/src/modules/order/order.service.ts` → `write()` calls in updateField/bulkUpdateField/importOrders/deleteOrder, `apps/web/src/components/orders/OrderLogTimelineDialog.tsx`, `packages/shared/dtos/order-log.dto.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Designer Task Workflow** (Leader + sub-designer, kanban, stats, bulk assign, productionErrorSource)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [`documents/FunctionDescription/DesignerTaskWorkflow.md`](documents/FunctionDescription/DesignerTaskWorkflow.md) | `apps/web/src/pages/designer/team/` + `apps/web/src/pages/designer/my-tasks/`, `apps/web/src/pages/orders/DesignerSummaryPanel.tsx`, `apps/web/src/pages/home/DesignerStatsTab.tsx`, `apps/web/src/pages/home/DesignerAssignBacklog.tsx` (backlog "Cần gán" + nút "Nhận về mình" self-claim cho role Designer), `apps/web/src/components/orders/AssignDesignerDialog.tsx`, `apps/web/src/components/orders/cells/{AssigneeSelectCell,ProductionErrorSelectCell,ProductionErrorOtherDialog,ErrorSourceCell}.tsx`, `apps/web/src/store/designerTeamStore.ts`, `apps/web/src/services/designer.ts` + `apps/web/src/services/order.ts` (`claimDesignerTasks`), `apps/api/src/modules/designer/` (toàn module: migration, team, task, stats), `apps/api/src/modules/order/order.service.ts` → `setProductionError()`, `bulkAssignDesigner()`, `claimDesignerTasks()` (self-claim), `getDesignerBreakdown()`, `backfillDesignerStatus()`, hook auto-rework + auto productionErrorCount in `updateField()`, `packages/shared/dtos/designer.dto.ts`, `packages/shared/enums/designer-status.ts`, `packages/shared/dtos/production-order.dto.ts` (designer fields + productionErrorSource/Count + bulk-assign + claim-designer-tasks + set-production-error DTOs), `apps/web/src/pages/designer/my-tasks/TaskCard.tsx` (badge ưu tiên + chip hạn dự kiến — xem `Orders.md §17`), `apps/web/src/pages/home/DesignerAssignBacklog.tsx` (badge ưu tiên + chip hạn dự kiến trong backlog "Cần gán" — xem `Orders.md §17.4`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Telegram Notification & Scheduled Reports** (import summary noti + cron 3 lần/ngày gửi báo cáo "Đơn 3 ngày liền kề" — 3 view/3 nút inline [🔄 Cập nhật][👤 Designer][🏭 Xưởng]: Stock out (`fulfillmentCompletedAt`)/tổng + 4 trạng thái thiết kế & tồn theo `designerFlowConds()` (`apps/api/src/utils/designer-flow.ts` — tách từ DesignerStatsService, số khớp 100% bảng "Tổng quan N ngày"/"Tất cả designer theo ngày", scope loại đơn hủy/chưa map xưởng/xưởng US), bucket `inProductionAt` giờ VN + section khách ưu tiên (`customer_priority_config`, hiện bất kể công tắc) → webhook `POST /v1/telegram/webhook` (secret token + allowlist chat_id + khóa in-flight, 2 người bấm liền thì người sau chờ); nút web Dashboard 1 click `POST /reports/run-now`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | [`documents/FunctionDescription/TelegramNotification.md`](documents/FunctionDescription/TelegramNotification.md) | `apps/api/src/modules/telegram-notification/` (service + 2 formatter `import-summary`/`daily-orders-report` + types `REPORT_CALLBACKS`), `apps/api/src/modules/scheduled-reports/` (service khóa `running` + onModuleInit setWebhook + controller + `telegram-webhook.controller.ts` + `aggregators/daily-orders-aggregator.ts` ($facet 4 nhánh days/customers/designers/factories) + `REPORT_DAY_COUNT`/`buildReportDayWindows`), `apps/api/src/modules/customer-assignment/customer-assignment.service.ts` → `getPriorityCustomers()`, `apps/api/src/modules/order/order.service.ts` → `sendImportSummaryNotification()` hook in `importOrders()`, `apps/api/src/shared/services/api-config.service.ts` → `telegram.*` (+ `webhookUrl`/`webhookSecret`) + `scheduledReports.enabled` getters, `packages/core/services/telegram.service.ts` (sendMessage + `replyMarkup` + `answerCallbackQuery` + `setWebhook`), `apps/web/src/pages/home/SendTelegramReportButton.tsx` + `apps/web/src/services/reports.ts`, env: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_NOTIFICATION_CHANNEL_ID`, `TELEGRAM_NOTIFICATION_ENABLED`, `SCHEDULED_REPORTS_ENABLED`, `TELEGRAM_WEBHOOK_URL`, `TELEGRAM_WEBHOOK_SECRET`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Scan Barcode → Gán lỗi nhanh** (workshop quét máy USB HID → dialog gán productionError + optional rework-back về stage trước; page riêng `/orders/scan-error` autoFocus input + recent scans. **Công nhân Fulfillment** (có `fulfillmentStage`): quét đơn đúng công đoạn → Enter Hoàn thành (tự start+complete) hoặc Báo lỗi qua `FulfillmentScanActionDialog`. **Báo lỗi ĐƠN XA**: quét đơn KHÔNG phải công đoạn mình / đã hoàn thành → vẫn hiện nút "Báo lỗi đơn này" → `OrderErrorScanDialog` gộp về 1 lần `setProductionError({target})`, đẩy về công đoạn trước + làm lại toàn chuỗi)                           | [`documents/FunctionDescription/ScanError.md`](documents/FunctionDescription/ScanError.md)                       | `apps/web/src/pages/orders/scan-error/` (page index.tsx + OrderErrorScanDialog.tsx + FulfillmentScanActionDialog.tsx), `apps/web/src/services/order.ts` → `getByProductionId()`, `apps/web/src/constants/{paths,routerConfig}.ts`, `apps/web/src/components/sidebar/Sidebar.tsx` (entry "Quét mã lỗi" trong group Orders), `apps/api/src/modules/order/order.controller.ts` → `GET /by-production-id/:code`, `apps/api/src/modules/order/order.service.ts` → `getByProductionId()` + `setProductionError` (field `target`) + `buildFulfillmentReworkBack()`, `packages/shared/dtos/production-order.dto.ts` → `GetOrderByProductionIdResDto` + `SetProductionErrorZod.target`, `packages/shared/constants/permission-catalog.ts` → `page.scan_error` + Fulfillment preset. Xem `documents/Plans/UniversalErrorReporting-PerPersonStageStats.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Fulfillment 6-Stage Workflow** (In→Ép→QC sau ép→May nhận vào→May xuất ra→Đóng hàng; đã bỏ `qc-sorting`+`qc-post-sew`, migrate đơn tồn qc-sorting→sew-in; 1 user per (factory,stage); rework-back về designer hoặc bất kỳ stage trước; UI kanban 4 cột + bottom drawer watching. **Tab "Đang chờ quay lại" (watching) = STAGE+XƯỞNG** (bỏ `byUserId` — mọi người giữ công đoạn đó đều thấy; sửa ĐỒNG THỜI `applyTabFilter` + `applyFulfillmentStatusFilter`). **Ô "Thống kê lỗi công đoạn"** (`StageErrorPanel.tsx` → `GET /fulfillment/stage-error-daily`) + badge lỗi tổng quát mọi cột trên `FulfillmentTaskCard`) | [`documents/FunctionDescription/FulfillmentWorkflow.md`](documents/FunctionDescription/FulfillmentWorkflow.md)   | `apps/api/src/modules/fulfillment/` (toàn module: task service + controller + module — 5 tab filter waiting/in-progress/rework/done/watching, watching stage-scoped), `apps/api/src/modules/order/order.entity.ts` (currentFulfillmentStage + fulfillmentStages 6 subdoc + fulfillmentTimeline + fulfillmentCompletedAt + makeEmptyStageState), `apps/api/src/modules/user/user.entity.ts` (fulfillmentStage + partial unique index `unique_factory_fulfillment_stage`), `apps/api/src/modules/user/user.service.ts` (Fulfillment validation + isDuplicateStageError E11000 handler + `getUserById`/`getMe` $project bao gồm fulfillmentStage — xem `Architecture/Common_Pitfalls.md §1`), `apps/api/src/modules/designer/designer-task.service.ts` (hook entry A: designer.complete → init print stage với waitingAt), `apps/api/src/modules/order/order.service.ts` (factory `buildFulfillmentEntrySet()` + hook entry B: toolResultNote='ok' qua updateField/bulkUpdateField/importRework, set waitingAt), `apps/web/src/pages/fulfillment/my-tasks/` (kanban index.tsx 4 cột + DnD + filter + bulk + FulfillmentTaskCard + ReworkBackDialog), `apps/web/src/pages/users/index.tsx` (stage dropdown auto-derive từ FULFILLMENT_STAGES khi role=Fulfillment), `apps/web/src/services/fulfillment.ts`, `apps/web/src/components/sidebar/Sidebar.tsx` (entry Task Fulfillment), `apps/web/src/constants/{paths,routerConfig}.ts`, `apps/web/src/main.tsx` (dayjs relativeTime + locale VI), `packages/shared/enums/fulfillment-stage.ts` (6 stage + status + transition action enum), `packages/shared/dtos/production-order.dto.ts` (fulfillment fields trong ProductionOrderZod + waitingAt + FulfillmentTransitionDto + GetFulfillmentMyTasksDto 5 tab + queue/stats DTOs + FulfillmentStagesZod 6 subkey), `packages/shared/dtos/user.dto.ts` (fulfillmentStage), `packages/shared/constants/permission-catalog.ts` (6 perms mới + Fulfillment preset), `apps/web/src/pages/fulfillment/my-tasks/FulfillmentTaskCard.tsx` (badge ưu tiên + chip hạn dự kiến — xem `Orders.md §17`) |
| **Soát tool** (nguồn lỗi thứ 3 `errorSource='tool-check'` — In báo "Thiếu file để in" → đẩy về Support thay vì designer/xưởng; tab Dashboard "Soát tool" CHỈ Support+Admin: 2 nhóm list cần-làm-lại/chưa-soát + thống kê lỗi theo sản phẩm/khách hàng)                                                                                                                                                                                                                                                                                                                                                                 | [`documents/FunctionDescription/ToolCheckWorkflow.md`](documents/FunctionDescription/ToolCheckWorkflow.md)       | `apps/web/src/pages/home/ToolCheckTab.tsx` (+ tab `tool-check` gate `page.tool_check` trong `index.tsx`, bên trái tab Designer), `apps/web/src/pages/workshop-config/CategoryEditor.tsx` (nút nguồn + `ErrorSourceBadge`), `apps/web/src/components/orders/cells/{ProductionErrorSelectCell,ErrorSourceCell,ColorBadgeSelectCell,SelectPopover,ProductionErrorOtherDialog}.tsx` (badge + type 'tool-check'), `apps/web/src/components/orders/workshopTableConfig.tsx` (row type), `apps/web/src/services/designer.ts` → `toolCheckOverview`, `apps/api/src/modules/order/order.service.ts` (`canReworkBackToSupport` + `buildDesignerReworkBackFromError(target)` + 3 call-site + `applyFulfillmentStatusFilter` waiting/watching + `markToolCheckDone()` nút "Đã soát xong" → `order.controller.ts` `POST /orders/:id/tool-check-done` + FE `services/order.ts` `markToolCheckDone`), `apps/api/src/modules/fulfillment/fulfillment-task.service.ts` (tab watching), `apps/api/src/modules/designer/designer-stats.service.ts` → `getToolCheckOverview()` + `designer-stats.controller.ts` → `GET /designer/tool-check-overview` (`@Auth` Support/Admin), `apps/api/src/modules/workshop-config/workshop-config.seed.ts` + `workshop-config.entity.ts` (errorSource 'tool-check' + seed "Thiếu file để in"), `packages/shared/dtos/{workshop-config,production-order,designer}.dto.ts` (ErrorSource enum + ToolCheckOverview DTOs), `packages/shared/constants/permission-catalog.ts` (`page.tool_check` + Support preset)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

| **Auto-gán Designer (chuỗi ưu tiên khách hàng → sản phẩm → xưởng)** (Admin cấu hình ở `/adm/settings/designer-assign`, 3 mức ưu tiên: **1) Khách hàng → designer** — kanban kéo thả card khách (bảng `customers`, khớp đơn qua `customerMatchKey(userSku,userEmail)`), gán thẳng BẤT KỂ xưởng; **2) Sản phẩm → designer** — kanban card Product Config (khớp đơn qua `productConfigId`), gán thẳng BẤT KỂ xưởng; kanban mức 1/2 **thả là auto-save ngay** (chuỗi hóa save, nút Lưu chỉ còn cần cho trọng số mức 3); **3) Theo xưởng** — mỗi xưởng có designer nào + trọng số %, 1 designer chỉ thuộc 1 xưởng, chia cân bằng tải theo trọng số (weighted least-loaded, đọc số đơn chưa xong từ DB — gán lẻ N=1 vẫn đúng tỉ lệ, đơn vừa gán mức 1/2 cộng vào tải trước khi chia); 1 khách/1 sản phẩm chỉ thuộc 1 designer; designer chỉ định tắt/đổi role → rơi xuống mức sau; sau soát tool xong `toolResultNote` có giá trị & != 'ok' → auto gán; hook `importRework` + `updateField('toolResultNote')` + `markToolCheckDone` + **báo lỗi nguồn designer trên đơn chưa ai ôm** (`setProductionError` / `updateField('productionError'/'productionErrorSource')` / rework-back target=designer ở `fulfillment-task.service.ts` — ứng viên `designerStatus='rework'`+assignee rỗng, gán xong GIỮ `rework` vào thẳng cột "Cần làm lại"); đơn ngoài cấu hình/chưa map xưởng để trống; **nút "Ghi nhớ cấu hình"** ở bảng "Cần gán designer" (CHỈ Admin/SuperAdmin) — gán ngay đơn đang tick + lưu mapping sản phẩm→designer mức 2 với hạn Hiện tại/1 ngày/7 ngày/Vĩnh viễn (`productExpiries`, lazy expiry: hết hạn engine bỏ qua + card tự về "Chưa gán")) | [`documents/FunctionDescription/DesignerAutoAssign.md`](documents/FunctionDescription/DesignerAutoAssign.md) | `apps/web/src/pages/settings/index.tsx`, `apps/web/src/components/settings/DesignerAssignmentConfig.tsx` (3 section) + `DesignerAssignKanban.tsx` (kanban dùng chung mức 1/2, chip hạn `expiryLabel`), `apps/web/src/components/orders/RememberAssignConfigDialog.tsx` (dialog "Ghi nhớ cấu hình" mở từ `DesignerAssignBacklog.tsx` + panel drill `DesignerDrillPanel.tsx`) + `AutoAssignPlanDialog.tsx` (dialog plan "Tự động gán" dùng chung 2 nơi), `apps/web/src/services/designerAssignment.ts` (+ `services/index.ts`), i18n `designerAutoAssign.json`, `apps/api/src/modules/designer-assignment/` (service + controller + module, `@Auth([Admin])`, lưu blob `system_configs` key `designer_assignment_config`, validate bất biến cả 3 mức), `apps/api/src/modules/order/order.service.ts` → `autoAssignAfterImport()` (public, route 3 mức) + `allocateByLoad()` + hook `importRework`/`updateField`/`setProductionError`/`markToolCheckDone`, `apps/api/src/modules/fulfillment/fulfillment-task.service.ts` → hook `transition()` rework-back designer (+ `fulfillment.module.ts` import OrderModule), `apps/api/src/modules/order/order.module.ts` (import SystemConfigModule + CustomerModule), `apps/api/src/app.module.ts` (đăng ký DesignerAssignmentModule), `packages/shared/dtos/designer-assignment.dto.ts` (`DESIGNER_ASSIGNMENT_CONFIG_KEY` + config DTOs `customers`/`products`/`factories`/`productExpiries` + `RememberProductAssignmentDto` — endpoint `POST /designer-assignment/remember-products`). Designer KHÔNG có `factoryId` — liên kết designer↔xưởng chỉ ở cấu hình này. |
| **Ưu tiên gán xưởng theo khách hàng** (Admin bật/tắt ở `/adm/settings/customer-factory`; bật → import đơn mới, đơn khớp cặp (userSku,userEmail) của khách đã gán → ép `factoryId` theo xưởng khách, bỏ qua product config; khách chưa gán vẫn theo product config; 1 khách 1 xưởng; chỉ ép factoryId, giữ machineType/fabric/toolResult; áp cả đơn chưa map. Bảng khách `customers` build từ orders qua nút Sync + nút Thêm tay. **Tier khách VIP 0..5 + khách lẻ (null), chỉ hiển thị/quản lý**: nút "Danh sách khách" mở `CustomerListDialog.tsx` (search + filter tier + badge màu đổi tier `PATCH /customers/:id/tier`) + Import tier từ file `TÊN TÀI KHOẢN | VIP n` (`POST /customers/import-tiers`, khớp userSku case-insensitive mọi email, không tự tạo khách). **Ưu tiên đơn theo khách (§8)**: tab riêng `/adm/settings/customer-priority` (`CustomerPriorityConfig.tsx`) kéo khách vào 3 cột `OrderPriority` 1..3 → `importOrders` auto-gán `priority` cho đơn khớp khách, CHỈ khi đơn chưa có priority (không đè chỉnh tay, không backfill), độc lập tier VIP; config blob `customer_priority_config` + `GET/PUT /customer-assignment/priority-config` + service `getPriorityConfig`/`savePriorityConfig`/`getPriorityImportOverride`, kanban dùng chung `CustomerFactoryKanban.tsx` (prop `columns`), i18n namespace `customerPriority`) | [`documents/FunctionDescription/CustomerFactoryAssignment.md`](documents/FunctionDescription/CustomerFactoryAssignment.md) | `apps/web/src/pages/settings/index.tsx`, `apps/web/src/components/settings/CustomerAssignmentConfig.tsx` (kanban kéo thả gán khách↔xưởng qua `CustomerFactoryKanban.tsx`: cột "Chưa gán" + 1 cột/xưởng, sort VIP 0→5 rồi khách lẻ A→Z, search mọi cột, dirty guard confirm khi thoát chưa lưu) + `CustomerListDialog.tsx`, `apps/web/src/services/customer.ts` + `customerAssignment.ts` (+ `services/index.ts`), `apps/api/src/modules/customer/` (entity `customers` + unique index `{userSku,userEmail}` + field `tier` + service `list`/`create`/`sync`/`updateTier`/`importTiers` + controller `GET/POST /customers` + `POST /customers/sync` + `PATCH /customers/:id/tier` + `POST /customers/import-tiers`), `apps/api/src/modules/customer-assignment/` (service `getConfig`/`saveConfig`/`getImportOverride` + controller `GET/PUT /customer-assignment/config`, `@Auth([Admin])`, blob `system_configs` key `customer_assignment_config`), `apps/api/src/modules/order/order.service.ts` → hook trong `importOrders` (`getImportOverride` + ép `factoryId` qua `customerMatchKey`), `apps/api/src/modules/order/order.module.ts` (import CustomerAssignmentModule), `apps/api/src/app.module.ts` (đăng ký CustomerModule + CustomerAssignmentModule), `packages/shared/dtos/customer.dto.ts` (`customerMatchKey` + `CUSTOMER_TIERS`/`CustomerTierZod` + Customer/Tier DTOs) + `customer-assignment.dto.ts` (`CUSTOMER_ASSIGNMENT_CONFIG_KEY` + config DTOs). |

| **Stage Error Catalog** (danh mục lỗi theo công đoạn + quét 2 bước hands-free: quét đơn `N-…` → quét `OK` tự hoàn thành công đoạn HOẶC quét barcode lỗi `E-<code>` tự gán lỗi + đẩy về đích cấu hình sẵn; công nhân Fulfillment tự thêm lỗi cho stage mình + in bảng barcode Code128 dán trạm (đổi từ QR — nhiều máy quét chỉ đọc 1D, payload không đổi); row lưu trong workshop_config category=production_error với `stage`+`reworkTarget`, errorSource BE tự suy) | [`documents/FunctionDescription/StageErrorCatalog.md`](documents/FunctionDescription/StageErrorCatalog.md) | `apps/web/src/pages/orders/stage-errors/index.tsx` (trang danh mục + barcode `react-barcode` + in sheet), `apps/web/src/utils/scanCodes.ts` (`parseScanCode`/`resolveErrorScan`/beep WebAudio), `apps/web/src/pages/orders/scan-error/{index,FulfillmentScanActionDialog,OrderErrorScanDialog}.tsx` (buffer bắt máy quét trong dialog), `apps/web/src/services/workshopConfig.ts` (`listStageErrors`/`createStageError`/`updateStageError`), `apps/web/src/constants/{paths,routerConfig}.ts` + `apps/web/src/components/sidebar/Sidebar.tsx` (entry "Danh mục lỗi công đoạn"), `apps/api/src/modules/workshop-config/` (entity `stage`/`reworkTarget` + service `listStageErrors`/`createStageError`/`updateStageError` + controller `GET/POST /workshop-config/stage-errors` + `PATCH .../:id`), `packages/shared/dtos/workshop-config.dto.ts` (`StageErrorReworkTargetZod` + StageError DTOs), `packages/shared/constants/permission-catalog.ts` (`page.stage_errors` + Fulfillment preset) |
| **Customer Portal** (root router `/customer` — khách hàng bên ngoài tự đăng ký/đăng nhập, đặt đơn CHỈ thông tin cơ bản (default sản xuất tái dùng luồng import nội bộ), xem danh sách đơn + tiến trình đơn của chính mình + **Catalog `/customer/catalog`** (CustomerPortal.md §7: giá tham khảo đã áp `Promotion` theo tier khách, ẩn tuyệt đối `cost`/`nonShipCost`, CHƯA đổi form đặt đơn). Tài khoản = bảng `customers` dùng chung với tính năng gán xưởng theo khách — record sync/thêm tay có `password=''`, khách tự đăng ký sẽ "claim" lại đúng record theo (userSku,userEmail). Auth dùng chung JWT/AuthModule nhân viên qua `RoleType.Customer`, KHÔNG dùng permission-catalog nội bộ) | [`documents/FunctionDescription/CustomerPortal.md`](documents/FunctionDescription/CustomerPortal.md) | `apps/web/src/pages/customer/` (`login/`, `register/`, `orders/{index,new,track}.tsx`, `catalog/index.tsx`), `apps/web/src/layouts/customerLayout/CustomerLayout.tsx`, `apps/web/src/store/customerAuthStore.ts`, `apps/web/src/services/customerPortal.ts` (+ `customerCatalog.getCatalog`), `apps/web/src/apis/index.tsx` (phân luồng token theo URL `/customer/...`), `apps/api/src/modules/customer-portal/` (toàn module: `customer-auth.controller.ts`, `customer-order.controller.ts`, `customer-order.service.ts`, `customer-catalog.controller.ts`, `customer-catalog.service.ts` — import `PromotionModule`), `apps/api/src/modules/customer/customer.entity.ts` (+ `password`/`fullName`/`phone`/`status`) + `customer.service.ts` (`register()`/`validateLogin()`/`getById()`/`toSafeCustomer()`), `apps/api/src/modules/auth/jwt.strategy.ts` (branch `RoleType.Customer`) + `auth.module.ts` (import CustomerModule), `packages/shared/enums/role-type.ts` (`RoleType.Customer`), `packages/shared/dtos/customer.dto.ts` (Register/Login DTOs) + `production-order.dto.ts` (`PlaceCustomerOrderDto` + `CustomerOrderSummary` + track DTOs) + `product-config.dto.ts` (`CustomerCatalogItemZod`/`CustomerCatalogVariationZod`/`GetCustomerCatalogDto`), `apps/web/src/components/customer/{CatalogProductCard,NotificationBell}.tsx` + `apps/web/src/components/common/FileUrlOrUploadInput.tsx`, **Thông báo Admin→khách hàng (CustomerPortal.md §8):** `apps/api/src/modules/customer-notification/` (module riêng, KHÔNG chung `NotificationEntity` nhân viên) + `apps/web/src/components/settings/CustomerNotificationSender.tsx` (mục `/adm/settings/customer-notify`) + `packages/shared/dtos/customer-notification.dto.ts` |
| **Sidebar Badges** (số đếm việc-cần-làm trên 3 entry sidebar: Nhật ký bù lỗi đỏ theo góc nhìn chặng viewer + Designer vàng chưa-gán/đỏ tồn 7 ngày (Designer thường: tồn của chính họ) + Soát tool vàng cần-làm-lại/đỏ chưa-soát; tooltip theo role, polling 60s + refetch debounce sau mutation qua axios interceptor; mọi số MIRROR công thức trang tương ứng) | [`documents/FunctionDescription/SidebarBadges.md`](documents/FunctionDescription/SidebarBadges.md) | `apps/web/src/components/sidebar/Sidebar.tsx` (`BadgePill`/`aggregateBadges`/`badgeMap` + polling), `apps/web/src/store/sidebarBadgeStore.ts`, `apps/web/src/apis/index.tsx` (`isBadgeAffectingMutation` bump refresh), `apps/web/src/services/designer.ts` (`sidebarCounts`), i18n `layout.json` (`sidebar.badges.*`), `apps/api/src/modules/designer/designer-stats.controller.ts` (`GET /designer/sidebar-counts`, `SIDEBAR_COUNT_ROLES`) + `designer-stats.service.ts` (`getSidebarCounts()` + `designerFlowConds()` dùng chung với `getDailyOverview`), `apps/api/src/modules/order/order.service.ts` (`countErrorLogTodo()` + `buildErrorLogBaseFilter()` dùng chung với `getErrorLog`), `apps/api/src/modules/designer/designer.module.ts` (import OrderModule), `packages/shared/dtos/designer.dto.ts` (`SidebarCountsZod`/`GetSidebarCountsResDto`) |
| **Landing** (trang chủ public `/` — **UX hướng KHÁCH HÀNG ĐẶT ĐƠN**, không hướng sản xuất: CTA chính xuyên suốt là "Đặt đơn ngay" (`CUSTOMER_REGISTER`), lối vào nhân viên cố ý hạ xuống chữ nhỏ ở CTA cuối + footer. KHÔNG gate auth, KHÔNG gọi API, 100% tĩnh. Nhận diện kế thừa onosglobal.com: tím `#6f26c2` = `brand.600`, band tối `#2b2739` = `ink.800/900`, font tiêu đề `Lexend Deca` = `font-display`, CTA pill UPPERCASE, nét gạch chân vẽ tay `Swoosh`. 9 section: Hero → Capabilities → ProblemSolution → HowItWorks → Showcase → Benefits → Trust → LeadershipTeam → FinalCta. **TUYỆT ĐỐI không đưa dữ liệu vận hành thật vào trang này** — nhãn mockup đã cố ý tổng quát hoá (không tên 8 chặng/6 công đoạn thật, không `productionId` thật, không mã lỗi thật, không giá), và KHÔNG nêu số cấu hình hệ thống; bảng "đã loại bỏ những gì" ở Landing.md §8. Mục **LeadershipTeam** dùng tên/chức danh thật từ onosglobal.com — người CHƯA có ảnh xác thực hiện vòng chữ cái đầu, **cấm lấp bằng ảnh mẫu theme/stock** (Landing.md §5); Tien Ha đã được yêu cầu bỏ khỏi trang) | [`documents/FunctionDescription/Landing.md`](documents/FunctionDescription/Landing.md) | `apps/web/src/pages/landing/` (`index.tsx` + `sections/{Hero,Capabilities,ProblemSolution,HowItWorks,Showcase,Benefits,Trust,LeadershipTeam,FinalCta}` + `mockups/{CustomerTracking,CatalogGrid,LifecycleFunnel}` + `components/AppFrame.tsx`), **primitive dùng chung mọi trang public ở `apps/web/src/components/public/`** (`useInView`/`Reveal`/`Swoosh`/`SectionHeading`/`PillLink`/`PublicHeader`/`PublicFooter`/`BackToTop`) — thêm trang public mới thì TÁI DÙNG bộ này, i18n namespace `landing` (`src/i18n/locales/{vi,en}/landing.json`), `apps/web/tailwind.config.js` (token `brand.*`/`ink.*` + `fontFamily.display` + keyframe `marquee`), `apps/web/index.html` (gộp `Lexend+Deca` vào request Google Fonts sẵn có), route khai báo thẳng trong `apps/web/src/App.tsx` (ngoài `PrivateRoute`) |
| **Catalog công khai** (`/catalog` + `/catalog/:id` — khách CHƯA đăng nhập xem hàng rồi mới đăng ký đặt đơn; hiện **giá niêm yết** `retailPrice`, **KHÔNG** áp khuyến mãi theo tier VIP và **KHÔNG BAO GIỜ** lộ `cost`/`nonShipCost`. Service dùng chung lõi với catalog khách đã đăng nhập qua cờ `applyPromotions`) | [`documents/FunctionDescription/Catalog.md`](documents/FunctionDescription/Catalog.md) | `apps/web/src/pages/catalog/{index,detail}.tsx`, `apps/web/src/components/public/{PublicProductCard.tsx,catalogPrice.ts}`, `apps/web/src/services/publicCatalog.ts`, i18n namespace `catalog`, `apps/api/src/modules/customer-portal/public-catalog.controller.ts` + `customer-catalog.service.ts` (`getPublicCatalog`/`getPublicCatalogItem` + lõi `listCatalog`/`findCatalogItem`), DTO TÁI DÙNG `GetCustomerCatalog*` ở `packages/shared/dtos/product-config.dto.ts` |
| **Careers** (landing page tuyển dụng public, route `/company/careers`, nội dung LUÔN tiếng Anh (không đổi theo toggle VI/EN), trang tĩnh không API — nội dung theo mẫu tờ rơi thật của Onos Group: hero "We're Hiring! Machine Operator" + badge số vị trí + info strip Location/Pay/Shift/Safety + Job Responsibilities/Qualifications/Compensation & Benefits + Work Schedule/Employment Type + Apply Today qua email/WhatsApp/website) | [`documents/FunctionDescription/Careers.md`](documents/FunctionDescription/Careers.md) | `apps/web/src/pages/company/careers/index.tsx`, `apps/web/src/App.tsx` (route public không qua `PrivateRoute`), `apps/web/src/constants/paths.ts` (`COMPANY_CAREERS`), i18n namespace `careers` (`src/i18n/locales/{vi,en}/careers.json` — 2 file nội dung giống nhau) |

### Cross-cutting docs (không nằm trong feature mapping)

| Doc                                                                                      | Khi nào đọc                                                                                                                                                                                                                                                                                               |
| ---------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`documents/Architecture/Common_Pitfalls.md`](documents/Architecture/Common_Pitfalls.md) | **Trước khi** add field mới vào `UserEntity`, refactor enum trong production, viết const patch object với `new Date()`, hoặc sửa `packages/shared/`. Tổng hợp 5 bug pattern cross-cutting đã từng xảy ra với root cause + rule chung.                                                                     |
| [`documents/FunctionDescription/Orders.md`](documents/FunctionDescription/Orders.md) §20 | **Trước khi** thêm 1 trang danh sách/filter mới có entry sidebar, hoặc sửa `Sidebar.tsx`/`sidebarResetStore.ts`/`useSidebarResetSignal.ts`. Cơ chế "click lại menu sidebar đang active → tự xóa filter trang đó" (React Router không remount khi click Link trùng URL hiện tại) + bảng các trang đã wire. |
| [`documents/FunctionDescription/I18n.md`](documents/FunctionDescription/I18n.md) | **Trước khi** thêm text hiển thị người dùng mới ở bất kỳ trang/component nào trong `apps/web`, hoặc sửa `src/i18n/`/`src/store/languageStore.ts`. Quy tắc bắt buộc dùng `react-i18next` thay vì hardcode string (namespace theo feature, convert module-scope label map/zod schema thành factory function nhận `t`, cách xử lý label nguồn gốc `packages/shared`). |

### Doc file structure & quy tắc viết doc

> Template file mới + quy tắc viết doc: xem skill `write-feature-doc` (`.claude/skills/write-feature-doc/SKILL.md`) — tự load khi tạo file doc mới trong `documents/FunctionDescription/`.
